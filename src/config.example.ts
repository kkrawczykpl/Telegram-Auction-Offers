/**
 * BOT_TOKEN: Telegram token to control BOT.
 * help_msg: Message that is sent when /help is called
 * check_time: Time interval 
 */

export const config = {
    BOT_TOKEN: "telegram_token",
    help_msg: "type /help",
    check_time: 900
}